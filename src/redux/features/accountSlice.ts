const initialState = {
  balance: 0,
  loan: 0,
};

const accountReducer = (state = initialState, action) => {
  switch (action.type) {
    case 'asd':
      break;
    default:
      return state;
  }
};

export default accountReducer;
